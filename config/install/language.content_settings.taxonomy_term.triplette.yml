langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.triplette
  enforced:
    module:
      - silfi_triplette
id: taxonomy_term.triplette
target_entity_type_id: taxonomy_term
target_bundle: triplette
default_langcode: site_default
language_alterable: false
